header
	nav.navbar.navbar-default.navbar-fixed-top(role='navigation')
		div.container-fluid
			div.navbar-header
				button.navbar-toggle.collapsed(type='button' data-toggle='collapse' data-target='.navbar-collapse')
					span.sr-only Toggle navigation
					span.icon-bar
					span.icon-bar
					span.icon-bar
				a.navbar-brand(href='#') FPV Bundesliga
			div.collapse.navbar-collapse
				ul.nav.navbar-nav
					li
						a([routerLink]='["Events"]') Veranstaltungen
					li
						a([routerLink]='["Pilots"]') Piloten
					li
						a(href='/') Rangliste
					li
						a(href='/') Karte
					li
						a() Eingeloggt: {{loggedIn}} 
					li
						register( [loggedIn]='loggedIn', (onLogin)='onLogin($event)', #register='' )
				login( [loggedIn]='loggedIn', (onLogin)='onLogin($event)' #login='' )
div.container
	router-outlet